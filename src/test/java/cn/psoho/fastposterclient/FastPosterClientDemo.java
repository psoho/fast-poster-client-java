package cn.psoho.fastposterclient;import org.apache.commons.io.FileUtils;import java.io.File;import java.io.IOException;import java.util.HashMap;public class FastPosterClientDemo {    public static void main(String[] args) throws IOException {        // 创建海报客户端对象        FastPosterClient client = new FastPosterClient("https://poster.prodapi.cn/", "ApfrIzxCoK1DwNZOEJCwlrnv6QZ0PCdv");        // 构造海报参数        HashMap<String, String> params = new HashMap<>();        // 暂未指定任何动态参数        // 海报ID        String posterId = "25";        // 获取下载地址        String url = client.getUrl(posterId, params);        System.out.println("url=" + url);        // 保存到本地        client.saveToPath(url, "temp.png");        // base64测试        String b64 = new String(client.getData(url.replace("/view/", "/b64/")));        System.out.println(b64);        // 验证        String data = "<img src=\"data:image/jpg;base64," + b64 + "\"/>";        FileUtils.writeStringToFile(new File("b64.html"), data, "utf-8");    }}